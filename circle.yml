machine:
  services:
    - docker
  java:
    version: oraclejdk8
dependencies:
  override:
    - mvn -version
test:
  pre:
    - mvn -Ptile clean install
  override:
    - sleep 3610
    - mvn -Ptomcat -Ddocker.keepContainer clean install -e
