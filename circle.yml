machine:
  services:
    - docker
  java:
    version: oraclejdk8
dependencies:
  override:
    - mvn -version
test:
  pre:
    - mvn -Ptile clean install
  override:
    - mvn -Pjetty -Dserver.version=9 -Ddocker.keepContainer clean install -e
