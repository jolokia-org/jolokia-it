machine:
  services:
    - docker
  java:
    version: oraclejdk8
dependencies:
  override:
    - echo "No prefetch of dependencies"
test:
  pre:
    - mvn -Ptile clean install
  override:
    - mvn -Ptomcat -Ddocker.keepContainer clean install -X
